import React from "react";
import Component from "../components";

const AllCartrunkContainer: React.FC<any> = (props) => {
  return (
    <>
      <Component {...props} />
    </>
  );
};

export default AllCartrunkContainer;

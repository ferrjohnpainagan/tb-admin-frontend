import React from "react";
import Component from "../components";

const AllDeliveriesContainer: React.FC<any> = (props) => {
  return (
    <>
      <Component {...props} />
    </>
  );
};

export default AllDeliveriesContainer;

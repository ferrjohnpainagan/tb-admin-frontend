import React from "react";
import Component from "../components";

const BookingDetailsContainer: React.FC<any> = (props) => {
    return (
        <>
            <Component {...props} />
        </>
    );
};

export default BookingDetailsContainer;

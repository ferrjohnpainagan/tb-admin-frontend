import React from "react";
import Component from "../components";

const AllBookingsContainer: React.FC<any> = (props) => {
  return (
    <>
      <Component {...props} />
    </>
  );
};

export default AllBookingsContainer;

import React from 'react'
import Component from "../components"

const AuthContainer: React.FC<any> = (props) => {
  return (
    <>
      <Component {...props} />
    </>
  )
}

export default AuthContainer
